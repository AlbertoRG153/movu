import TripStatus from "@/components/customer/trip_status";

export default function TripStatusPage() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-between">
            <TripStatus />
        </main>
    );
}
